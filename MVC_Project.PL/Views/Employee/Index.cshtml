@model IEnumerable<EmployeeViewModel>
<div class="row justify-content-center align-items-center mt-5">
    <form class="row col-8 offset-2" asp-action="Index">
        <div class="col-8">
            <input type="text" class="form-control" name="searchInp" placeholder="Search By Name"/>

        </div>
        <div>
            <input type="submit" value="search" class="btn btn-success" />
        </div>

    </form>
</div>


<h1>All Employees</h1>
<br />
<br />
<br />

<a asp-controller="Employee" asp-action="Create" class=" btn btn-primary">Create New Employee</a>

@if (Model.Count() > 0)
{
    <table class="table table-hover table-striped">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(D => D.Name)</th>
                <th>@Html.DisplayNameFor(D => D.Age)</th>
                <th>@Html.DisplayNameFor(D => D.Address)</th>
                <th>@Html.DisplayNameFor(D => D.department)</th>
                <th>@Html.DisplayNameFor(D => D.Salary)</th>
                <th>@Html.DisplayNameFor(D => D.Emp_type)</th>
                <th>@Html.DisplayNameFor(D => D.IsActive)</th>
                <th>@Html.DisplayNameFor(D => D.Email)</th>

                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var departmwent in Model)
            {
                <tr>
                    <td>@departmwent.Name</td>
                    <td>@departmwent.Age</td>
                    <td>@departmwent.Address</td>
                    <td>@departmwent.department?.Name</td>
                    <td >@Html.DisplayFor(M=>departmwent.Salary)</td>
                    <td>@Html.DisplayFor(M => departmwent.Emp_type)</td>
                    <td>@Html.DisplayFor(M => departmwent.IsActive)</td>
                    <td>@Html.DisplayFor(M => departmwent.Email)</td>
                    <partial name="_ButtonsPartial" model="@departmwent.Id"></partial>

                </tr>

            }
        </tbody>
    </table>
}
else
{
    <div class="mt-5 alert alert-warning">
        <h3>There is no Employee</h3>
    </div>
}
